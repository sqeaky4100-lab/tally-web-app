A count — sidebar list totals, group headers, overdue markers.

```jsx
<Badge>8</Badge>
<Badge tone="danger">1</Badge>
```

`quiet` is the default and correct choice almost always: a bare tabular number in `--text-tertiary`. Reserve `danger` for genuinely overdue counts.
