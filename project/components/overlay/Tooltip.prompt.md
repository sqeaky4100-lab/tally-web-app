A dark label naming an unlabelled control. Toolbar glyphs only — never explanatory prose.

```jsx
<Tooltip label="New To-Do" shortcut="⌘N"><IconButton icon="plus" label="New To-Do" /></Tooltip>
```

Two to three words plus an optional shortcut. If a control needs a sentence, it needs a different design.
