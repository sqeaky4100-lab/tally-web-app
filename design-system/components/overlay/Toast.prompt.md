A transient confirmation carrying an undo — the only feedback pattern for bulk or irreversible-feeling actions.

```jsx
<Toast message="4 to-dos moved to Logbook" actionLabel="Undo" />
```

A dark pill floating over the content, bottom-centre. Never use a toast to report an error the user must act on — that belongs inline.
