An immediate on/off preference — settings rows, never form fields.

```jsx
<Switch checked={on} onChange={toggle} label="Show completed" />
```

On-state is green, deliberately distinct from the blue used for selection, so a settings sheet does not look like a list of selected rows.
